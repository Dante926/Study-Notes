```js
// 数据封装与解封装
/* 
    ①这里采用TCP/IP协议，五层划分模式
    ②应用层产出data -> 
    ③传输层:包裹 目标端口:源端口:data ->
    ④网络层:包裹 目标IP:源IP:传输层 ->
    ⑤数据链路层：目标Mac:源Mac:网络层 ->
    ⑥物理层：通过网卡调制将数据链路层包装好的数据调制成高低电压 ->
    ⑦传递到对方主机的网卡中：解调然后类似于上面的方向操作
*/
```
