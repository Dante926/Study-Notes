```js
// 文件读写与拷贝文件
/* 
    ①存在同步和异步区别
    ②该节只重点于异步
    ③文件操作API：
        //
            readFile:从指定文件中读取数据
            writeFile:向指定文件中写入数据
            appendFile:向指定文件中追加数据
            copyFile:复制文件
            watchFile:监听文件变化,并且可以当内容发生修改后执行指定回调函数
        //
*/
```